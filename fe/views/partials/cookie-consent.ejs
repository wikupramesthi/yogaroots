<div id="cookieConsent" class="fixed inset-x-0 bottom-0 z-[9999] hidden">
  <div class="px-4 pb-4 md:px-6 md:pb-6">
    <div class="max-w-5xl mx-auto">
      ```
      <div
        class="bg-white/95 backdrop-blur-xl border border-stone-200 rounded-[24px] shadow-[0_20px_60px_rgba(49,57,47,0.16)] overflow-hidden"
      >
        <!-- Main Content -->
        <div class="p-5 md:p-6">
          <div class="flex flex-col lg:flex-row lg:items-center gap-5">
            <!-- Shield Icon -->
            <div
              class="hidden sm:flex w-11 h-11 shrink-0 rounded-full bg-sage-50 text-sage-700 items-center justify-center"
            >
              <svg
                xmlns="http://www.w3.org/2000/svg"
                width="20"
                height="20"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                stroke-linecap="round"
                stroke-linejoin="round"
              >
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                <path d="m9 12 2 2 4-4"></path>
              </svg>
            </div>

            <!-- Content -->
            <div class="flex-1">
              <h3
                class="font-display text-xl font-semibold text-sage-900 mb-1.5"
              >
                Your privacy matters
              </h3>

              <p class="text-sm leading-relaxed text-stone-warm max-w-3xl mb-0">
                We use cookies to keep Yogaroots running smoothly, understand
                how our website is used, and improve your experience. You can
                choose which cookies you allow.
              </p>

              <div class="flex flex-wrap items-center gap-x-4 gap-y-2 mt-3">
                <a
                  href="/privacy-policy"
                  class="text-xs font-semibold text-sage-700 hover:text-sage-900 transition"
                >
                  Privacy Policy
                </a>

                <button
                  type="button"
                  id="openCookiePreferences"
                  class="text-xs font-semibold text-stone-warm hover:text-sage-700 transition"
                >
                  Cookie Preferences
                </button>
              </div>
            </div>

            <!-- Actions -->
            <div
              class="flex flex-col sm:flex-row lg:flex-col xl:flex-row gap-2 shrink-0"
            >
              <button
                type="button"
                id="rejectCookies"
                class="px-5 py-2.5 rounded-full border border-stone-200 text-stone-warm text-xs font-semibold hover:border-sage-300 hover:text-sage-700 transition whitespace-nowrap"
              >
                Reject Non-Essential
              </button>

              <button
                type="button"
                id="acceptCookies"
                class="px-6 py-2.5 rounded-full bg-sage-700 text-white text-xs font-semibold hover:bg-sage-800 transition whitespace-nowrap"
              >
                Accept All
              </button>
            </div>
          </div>
        </div>

        <!-- Cookie Preferences -->
        <div id="cookiePreferences" class="hidden border-t border-stone-200">
          <div class="p-5 md:p-6">
            <!-- Header -->
            <div class="flex items-center justify-between mb-5">
              <div>
                <h4 class="font-semibold text-sage-900 text-sm mb-1">
                  Cookie Preferences
                </h4>

                <p class="text-xs text-stone-warm mb-0">
                  Choose which types of cookies you allow.
                </p>
              </div>

              <!-- Close -->
              <button
                type="button"
                id="closeCookiePreferences"
                class="w-9 h-9 rounded-full border border-stone-200 flex items-center justify-center text-stone-warm hover:text-sage-700 hover:border-sage-300 transition"
                aria-label="Close cookie preferences"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  width="16"
                  height="16"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <line x1="18" y1="6" x2="6" y2="18"></line>
                  <line x1="6" y1="6" x2="18" y2="18"></line>
                </svg>
              </button>
            </div>

            <!-- Cookie Types -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
              <!-- Essential -->
              <div class="rounded-2xl border border-stone-200 bg-cream/50 p-4">
                <div class="flex items-center justify-between gap-4">
                  <div>
                    <h5 class="text-sm font-semibold text-sage-900 mb-1">
                      Essential Cookies
                    </h5>

                    <p class="text-xs leading-relaxed text-stone-warm mb-0">
                      Required for essential website functions such as security,
                      sessions, and preferences.
                    </p>
                  </div>

                  <span
                    class="shrink-0 text-[10px] font-bold uppercase tracking-wider text-sage-700 bg-sage-50 px-2.5 py-1.5 rounded-full"
                  >
                    Always On
                  </span>
                </div>
              </div>

              <!-- Analytics -->
              <div class="rounded-2xl border border-stone-200 bg-cream/50 p-4">
                <div class="flex items-center justify-between gap-4">
                  <div>
                    <h5 class="text-sm font-semibold text-sage-900 mb-1">
                      Analytics Cookies
                    </h5>

                    <p class="text-xs leading-relaxed text-stone-warm mb-0">
                      Help us understand how visitors use Yogaroots so we can
                      improve the website.
                    </p>
                  </div>

                  <!-- Toggle -->
                  <label
                    class="relative inline-flex items-center cursor-pointer shrink-0"
                  >
                    <input
                      type="checkbox"
                      id="analyticsCookies"
                      class="sr-only peer"
                    />

                    <div
                      class="w-10 h-6 bg-stone-200 rounded-full peer-checked:bg-sage-700 after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:after:translate-x-4"
                    ></div>
                  </label>
                </div>
              </div>
            </div>

            <!-- Save -->
            <div class="flex justify-end mt-5">
              <button
                type="button"
                id="saveCookiePreferences"
                class="px-6 py-2.5 rounded-full bg-sage-700 text-white text-xs font-semibold hover:bg-sage-800 transition"
              >
                Save Preferences
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  ```
</div>

<!-- Floating Cookie Settings Button -->

<button
  type="button"
  id="cookieSettingsButton"
  class="hidden fixed bottom-5 left-5 z-[9998] w-11 h-11 rounded-full bg-white border border-stone-200 shadow-lg text-sage-700 hover:bg-sage-50 transition"
  aria-label="Cookie settings"
>
  ```
  <svg
    xmlns="http://www.w3.org/2000/svg"
    width="18"
    height="18"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
  >
    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
    <path d="m9 12 2 2 4-4"></path>
  </svg>
  ```
</button>

<script>
  (function () {
    const STORAGE_KEY = "yogaroots_cookie_consent";

    const banner = document.getElementById("cookieConsent");
    const preferences = document.getElementById("cookiePreferences");

    const acceptButton = document.getElementById("acceptCookies");
    const rejectButton = document.getElementById("rejectCookies");

    const openPreferences = document.getElementById("openCookiePreferences");

    const closePreferences = document.getElementById("closeCookiePreferences");

    const savePreferences = document.getElementById("saveCookiePreferences");

    const analyticsToggle = document.getElementById("analyticsCookies");

    const settingsButton = document.getElementById("cookieSettingsButton");

    function getConsent() {
      try {
        return JSON.parse(localStorage.getItem(STORAGE_KEY));
      } catch (error) {
        return null;
      }
    }

    function applyConsent(consent) {
      if (typeof window.gtag === "function") {
        window.gtag("consent", "update", {
          analytics_storage: consent.analytics ? "granted" : "denied",

          ad_storage: "denied",

          ad_user_data: "denied",

          ad_personalization: "denied",
        });
      }
    }

    function saveConsent(analytics) {
      const consent = {
        essential: true,

        analytics: analytics,

        timestamp: new Date().toISOString(),
      };

      localStorage.setItem(STORAGE_KEY, JSON.stringify(consent));

      applyConsent(consent);

      banner.classList.add("hidden");

      preferences.classList.add("hidden");

      settingsButton.classList.remove("hidden");
    }

    function showBanner() {
      banner.classList.remove("hidden");

      settingsButton.classList.add("hidden");
    }

    function showPreferences() {
      banner.classList.remove("hidden");

      preferences.classList.remove("hidden");

      const consent = getConsent();

      analyticsToggle.checked = !!consent?.analytics;
    }

    function hidePreferences() {
      preferences.classList.add("hidden");
    }

    const existingConsent = getConsent();

    if (!existingConsent) {
      showBanner();
    } else {
      applyConsent(existingConsent);

      settingsButton.classList.remove("hidden");
    }

    acceptButton.addEventListener("click", function () {
      saveConsent(true);
    });

    rejectButton.addEventListener("click", function () {
      saveConsent(false);
    });

    openPreferences.addEventListener("click", function () {
      showPreferences();
    });

    closePreferences.addEventListener("click", function () {
      hidePreferences();
    });

    savePreferences.addEventListener("click", function () {
      saveConsent(analyticsToggle.checked);
    });

    settingsButton.addEventListener("click", function () {
      showPreferences();
    });
  })();
</script>
